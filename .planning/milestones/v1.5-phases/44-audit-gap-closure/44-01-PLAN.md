---
phase: 44-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/net/transport/socket/raw_api.zig
  - src/net/transport/socket/root.zig
  - src/net/transport/socket.zig
autonomous: true
requirements: [DOC-03]
gap_closure: true

must_haves:
  truths:
    - "ROADMAP.md Phase 41 progress row has v1.5 milestone column, 2/2 Plans Complete, Complete status, and 2026-02-21 date"
    - "ROADMAP.md Phase 41 plan list shows 41-01-PLAN.md checkbox as [x]"
    - "REQUIREMENTS.md traceability table shows [x] Satisfied for all 9 completed requirements"
    - "recvfromRaw and recvfromRaw6 functions no longer exist in the codebase"
    - "sendtoRaw and sendtoRaw6 remain functional and exported (they ARE called from sys_sendto)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 41 progress row and plan checkbox"
      contains: "| 41. Code Cleanup and Documentation | v1.5 | 2/2 | Complete | 2026-02-21 |"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All 9 completed requirement checkboxes marked satisfied"
      contains: "[x] **NET-01**"
    - path: "src/net/transport/socket/raw_api.zig"
      provides: "Only sendtoRaw, sendtoRaw6, and their helpers remain"
  key_links:
    - from: "src/net/transport/socket/root.zig"
      to: "src/net/transport/socket/raw_api.zig"
      via: "pub const re-exports"
      pattern: "sendtoRaw"
    - from: "src/net/transport/socket.zig"
      to: "src/net/transport/socket/root.zig"
      via: "pub const re-exports"
      pattern: "sendtoRaw"
---

<objective>
Close all audit-identified gaps so the v1.5 milestone can proceed toward completion.

Purpose: The v1.5 milestone audit found 3 documentation tracking inconsistencies and 1 dead code issue from prior phases. These are the final Phase 41 self-referential tracking gaps (ROADMAP row malformed, REQUIREMENTS checkboxes stale) plus orphaned recvfromRaw/recvfromRaw6 functions that are never called from any syscall handler.

Output: Corrected ROADMAP.md and REQUIREMENTS.md tracking state, dead recv functions removed from raw_api.zig and their re-exports cleaned from root.zig and socket.zig.
</objective>

<execution_context>
@/Users/whit3rabbit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/whit3rabbit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/41-code-cleanup-and-documentation/41-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md Phase 41 progress row and plan checkbox</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Fix three issues in `.planning/ROADMAP.md`:

1. **Progress table row (line 205)**: Replace the malformed Phase 41 row:
   - Current: `| 41. Code Cleanup and Documentation | 1/2 | Complete    | 2026-02-21 | - |`
   - Correct: `| 41. Code Cleanup and Documentation | v1.5 | 2/2 | Complete | 2026-02-21 |`
   - The current row is missing the v1.5 milestone column and shows 1/2 instead of 2/2. Match the 5-column format used by all other rows (Phase | Milestone | Plans Complete | Status | Completed).

2. **Plan 41-01 checkbox (line 121)**: Change `- [ ] 41-01-PLAN.md` to `- [x] 41-01-PLAN.md`. Plan 01 was completed in commit 10666c2.

3. **Phase 41 Plans count (line 119)**: Change `**Plans**: 2 plans` to `**Plans:** 2/2 plans complete` to match the format used by Phase 40 on line 104.

4. **Phase 44 entry**: Update the Phase 44 section to show the plan list:
   - Update `**Plans**: TBD` to `**Plans:** 1 plan`
   - Add plan list: `Plans:\n- [ ] 44-01-PLAN.md -- Fix ROADMAP/REQUIREMENTS tracking and remove recvfromRaw dead code`

Do NOT touch Phase 42 or 43 entries (they are not started).
  </action>
  <verify>
Verify with grep:
- `grep "41\. Code Cleanup" .planning/ROADMAP.md` should show `| v1.5 | 2/2 | Complete | 2026-02-21 |`
- `grep "41-01-PLAN" .planning/ROADMAP.md` should show `[x]`
- `grep "44-01-PLAN" .planning/ROADMAP.md` should show `[ ]`
  </verify>
  <done>Phase 41 progress row has correct 5-column format with v1.5 milestone, 2/2 plans, and both plan checkboxes marked [x]. Phase 44 shows 1 plan in plan list.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability for all 9 completed requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/REQUIREMENTS.md` to reflect the 9 requirements that are already satisfied (verified by the audit):

**Requirement checkboxes (lines 10-24):** Change `- [ ]` to `- [x]` for:
- NET-01 (line 10)
- NET-02 (line 11)
- NET-03 (line 12)
- NET-04 (line 13)
- CLN-01 (line 17)
- CLN-02 (line 18)
- DOC-01 (line 22)
- DOC-02 (line 23)
- DOC-03 (line 24)

**Traceability table (lines 48-59):** Change `Pending` to `Satisfied` for all 9 requirements listed above. Also update DOC-03's Phase column from `Phase 44` to `Phase 41, 44` since both phases addressed it.

Do NOT touch TST-01, TST-02, TST-03 -- those remain Pending (phases 42/43 not started).

Update the "Last updated" line at the bottom of the file to reflect today's date and reason.
  </action>
  <verify>
Verify with grep:
- `grep -c '\- \[x\]' .planning/REQUIREMENTS.md` should return 9
- `grep -c '\- \[ \]' .planning/REQUIREMENTS.md` should return 3 (TST-01, TST-02, TST-03)
- `grep -c 'Satisfied' .planning/REQUIREMENTS.md` should return 9
- `grep -c 'Pending' .planning/REQUIREMENTS.md` should return 3
  </verify>
  <done>All 9 completed requirements show [x] checkboxes and Satisfied status. Only TST-01, TST-02, TST-03 remain as [ ] Pending.</done>
</task>

<task type="auto">
  <name>Task 3: Remove dead recvfromRaw and recvfromRaw6 functions and re-exports</name>
  <files>
    src/net/transport/socket/raw_api.zig
    src/net/transport/socket/root.zig
    src/net/transport/socket.zig
  </files>
  <action>
The audit found that `recvfromRaw` and `recvfromRaw6` are never called from any syscall handler. The SOCK_RAW recv path in sys_recvfrom (net.zig:686-688) routes through `udp_api.recvfromIp`, which independently handles MSG_DONTWAIT and MSG_PEEK. The recvfromRaw/recvfromRaw6 functions are exported but have zero callers.

The `sendtoRaw` and `sendtoRaw6` functions ARE called from sys_sendto (net.zig:507, 532) and must be preserved.

**In `src/net/transport/socket/raw_api.zig`:**
- Delete the `recvfromRaw` function (lines 119-195, the `pub fn recvfromRaw(` block including its doc comment on lines 119-122)
- Delete the `recvfromRaw6` function (lines 301-378, the `pub fn recvfromRaw6(` block including its doc comment on lines 301-304)
- Keep `sendtoRaw` (lines 25-117) and `sendtoRaw6` (lines 197-299) intact

**In `src/net/transport/socket/root.zig`:**
- Delete line 146: `pub const recvfromRaw = raw_api.recvfromRaw;`
- Delete line 148: `pub const recvfromRaw6 = raw_api.recvfromRaw6;`
- Keep `pub const sendtoRaw = raw_api.sendtoRaw;` (line 145)
- Keep `pub const sendtoRaw6 = raw_api.sendtoRaw6;` (line 147)

**In `src/net/transport/socket.zig`:**
- Delete line 143: `pub const recvfromRaw = root.recvfromRaw;`
- Delete line 145: `pub const recvfromRaw6 = root.recvfromRaw6;`
- Keep `pub const sendtoRaw = root.sendtoRaw;` (line 142)
- Keep `pub const sendtoRaw6 = root.sendtoRaw6;` (line 144)

After removal, verify the build compiles. Run: `zig build -Darch=x86_64 2>&1 | head -20`

If the build fails because something references recvfromRaw that we missed, search with `grep -rn "recvfromRaw" src/` and fix the reference.
  </action>
  <verify>
Verify:
- `grep -rn "recvfromRaw" src/` should return zero matches
- `grep -rn "sendtoRaw" src/` should still return matches in raw_api.zig, root.zig, socket.zig, and net.zig
- `zig build -Darch=x86_64` should compile without errors
  </verify>
  <done>recvfromRaw and recvfromRaw6 are removed from all 3 files. sendtoRaw and sendtoRaw6 remain functional. Build passes.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, verify against Phase 44 success criteria:

1. `grep "41\. Code Cleanup" .planning/ROADMAP.md` shows `| v1.5 | 2/2 | Complete | 2026-02-21 |`
2. `grep -c 'Satisfied' .planning/REQUIREMENTS.md` returns 9; `grep -c 'Pending' .planning/REQUIREMENTS.md` returns 3
3. `grep -rn "recvfromRaw" src/` returns zero matches; `zig build -Darch=x86_64` compiles cleanly
</verification>

<success_criteria>
1. ROADMAP.md Phase 41 progress row matches 5-column format with v1.5 milestone, 2/2 Plans Complete, and 2026-02-21 date
2. ROADMAP.md Plan 41-01 checkbox is [x]
3. REQUIREMENTS.md has 9 [x] Satisfied requirements and 3 [ ] Pending requirements
4. recvfromRaw and recvfromRaw6 are fully removed from raw_api.zig, root.zig, and socket.zig
5. sendtoRaw and sendtoRaw6 remain in place and the x86_64 build compiles
</success_criteria>

<output>
After completion, create `.planning/phases/44-audit-gap-closure/44-01-SUMMARY.md`
</output>
