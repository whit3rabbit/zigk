---
phase: 33-timer-resolution-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arch/x86_64/kernel/apic/root.zig
  - src/arch/aarch64/root.zig
  - src/kernel/proc/sched/scheduler.zig
  - src/kernel/sys/syscall/process/scheduling.zig
  - src/kernel/sys/syscall/misc/posix_timer.zig
  - src/kernel/sys/syscall/misc/sysinfo.zig
  - src/kernel/sys/syscall/io/timerfd.zig
autonomous: true
requirements: [PTMR-02]

must_haves:
  truths:
    - "Scheduler tick fires at 1000Hz (1ms) on both x86_64 and aarch64"
    - "POSIX timers decrement at 1ms granularity instead of 10ms"
    - "clock_nanosleep converts durations using 1ms tick period"
    - "clock_getres reports 1ms resolution for CLOCK_REALTIME and CLOCK_MONOTONIC"
    - "sysinfo uptime calculation uses 1000 ticks per second"
  artifacts:
    - path: "src/arch/x86_64/kernel/apic/root.zig"
      provides: "LAPIC timer at 1000Hz"
      contains: "enablePeriodicTimer(1000"
    - path: "src/arch/aarch64/root.zig"
      provides: "Generic timer at 1000Hz"
      contains: "pit.init(1000)"
    - path: "src/kernel/proc/sched/scheduler.zig"
      provides: "1ms tick constants in processIntervalTimers and setAlarm"
      contains: "TICK_MICROS: u64 = 1000"
    - path: "src/kernel/sys/syscall/process/scheduling.zig"
      provides: "1ms tick_ns and clock_getres"
      contains: "tv_nsec = 1_000_000"
    - path: "src/kernel/sys/syscall/misc/posix_timer.zig"
      provides: "1ms TICK_NS constant"
      contains: "TICK_NS: u64 = 1_000_000"
  key_links:
    - from: "src/arch/x86_64/kernel/apic/root.zig"
      to: "src/kernel/proc/sched/scheduler.zig"
      via: "LAPIC timer fires timerTick at 1000Hz"
      pattern: "enablePeriodicTimer\\(1000"
    - from: "src/arch/aarch64/root.zig"
      to: "src/kernel/proc/sched/scheduler.zig"
      via: "Generic timer fires timerTick at 1000Hz"
      pattern: "pit\\.init\\(1000\\)"
    - from: "src/kernel/proc/sched/scheduler.zig"
      to: "src/kernel/sys/syscall/misc/posix_timer.zig"
      via: "processIntervalTimers uses matching TICK_MICROS"
      pattern: "TICK_MICROS.*1000"
---

<objective>
Increase the scheduler tick frequency from 100Hz (10ms) to 1000Hz (1ms) across both architectures, updating all core timer and time syscall code to use the new tick period.

Purpose: This is the foundational change for Phase 33. All timer resolution improvements depend on the hardware tick firing more frequently and the kernel math using the correct constants.

Output: Both x86_64 and aarch64 timers fire at 1000Hz. Scheduler, POSIX timers, clock_nanosleep, clock_getres, and sysinfo all use 1ms tick arithmetic.
</objective>

<execution_context>
@/Users/whit3rabbit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/whit3rabbit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-timer-capacity-expansion/32-01-SUMMARY.md

Key source files to understand before modifying:
@src/arch/x86_64/kernel/apic/root.zig (line ~207: enablePeriodicTimer(100, ...))
@src/arch/x86_64/kernel/apic/lapic.zig (enablePeriodicTimer function: count = ticks_per_ms * 1000 / freq_hz)
@src/arch/aarch64/root.zig (line ~285: pit.init(100))
@src/arch/aarch64/kernel/timing.zig (startPeriodicTimer: timer_ticks_per_interval = freq / freq_hz)
@src/kernel/proc/sched/scheduler.zig (TICK_MICROS=10000, setAlarm: *100 / /100, load_last_update: 500 ticks)
@src/kernel/sys/syscall/process/scheduling.zig (tick_ns=10_000_000, clock_getres=10ms, getMonotonicTime fallback *10)
@src/kernel/sys/syscall/misc/posix_timer.zig (TICK_NS=10_000_000)
@src/kernel/sys/syscall/misc/sysinfo.zig (ticks / 100)
@src/kernel/sys/syscall/io/timerfd.zig (10ms granularity comment)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change hardware timer frequency to 1000Hz on both architectures</name>
  <files>
    src/arch/x86_64/kernel/apic/root.zig
    src/arch/aarch64/root.zig
  </files>
  <action>
In `src/arch/x86_64/kernel/apic/root.zig` (initTimer function around line 207-214):
- Change `lapic.enablePeriodicTimer(100, lapic.TIMER_VECTOR)` to `lapic.enablePeriodicTimer(1000, lapic.TIMER_VECTOR)`
- Update the log message from "100Hz" to "1000Hz" (two occurrences: line ~207 comment and line ~214 info log)

In `src/arch/aarch64/root.zig` (pit.init around line 285):
- Change `pit.init(100)` to `pit.init(1000)`
- Update the comment from "100Hz" to "1000Hz"

NOTE: The underlying enablePeriodicTimer (LAPIC) calculates `count = (timer_ticks_per_ms * 1000) / freq_hz`, so passing 1000 gives `count = timer_ticks_per_ms`, which is correct for 1ms period. The aarch64 startPeriodicTimer calculates `timer_ticks_per_interval = system_counter_freq / freq_hz`, which also works correctly with 1000.
  </action>
  <verify>
`zig build -Darch=x86_64` and `zig build -Darch=aarch64` both compile without errors.
  </verify>
  <done>Both architectures configure their hardware timers at 1000Hz instead of 100Hz. Log messages reflect the new frequency.</done>
</task>

<task type="auto">
  <name>Task 2: Update scheduler, time syscalls, and POSIX timer constants from 10ms to 1ms</name>
  <files>
    src/kernel/proc/sched/scheduler.zig
    src/kernel/sys/syscall/process/scheduling.zig
    src/kernel/sys/syscall/misc/posix_timer.zig
    src/kernel/sys/syscall/misc/sysinfo.zig
    src/kernel/sys/syscall/io/timerfd.zig
  </files>
  <action>
**src/kernel/proc/sched/scheduler.zig** (processIntervalTimers, around line 1039):
- Change `const TICK_MICROS: u64 = 10000;` to `const TICK_MICROS: u64 = 1000;`
- Update the comment from "10ms per tick (100 Hz)" to "1ms per tick (1000 Hz)"

**src/kernel/proc/sched/scheduler.zig** (setAlarm, around line 180-206):
- Line ~180 comment: change "max_u64 / 100" to "max_u64 / 1000"
- Line ~191-192: change `(remaining_ticks + 99) / 100` to `(remaining_ticks + 999) / 1000` and update comment to "1000Hz ticks to seconds"
- Line ~202 comment: change "tick freq is 100Hz" to "tick freq is 1000Hz"
- Line ~203: change `std.math.maxInt(u64) / 100` to `std.math.maxInt(u64) / 1000`
- Line ~206: change `clamped_seconds * 100` to `clamped_seconds * 1000` and update comment to "1000 ticks per second"

**src/kernel/proc/sched/scheduler.zig** (load average update, around line 811):
- Change the comparison from `>= 500` to `>= 5000` (5 seconds at 1000Hz instead of 100Hz)
- The updateLoadAverages function's EXP constants are based on 5-second intervals and don't need changing, since the call frequency stays at every 5 seconds

**src/kernel/sys/syscall/process/scheduling.zig** (clock_nanosleep_internal, around lines 451, 468, 487):
- Lines 451, 468: change `const tick_ns: u64 = 10_000_000;` to `const tick_ns: u64 = 1_000_000;` (1ms in nanoseconds)
- Line 487 (getCurrentTimeNs): change `return ticks * 10_000_000;` to `return ticks * 1_000_000;` and update comment to "1ms per tick"

**src/kernel/sys/syscall/process/scheduling.zig** (getMonotonicTime fallback, around lines 763-770):
- Line 765: change `ticks *| 10` to `ticks *| 1` (or just `ticks`, since each tick is now 1ms). Actually more readable: keep the multiply pattern but the value is 1. Simplest: `const ms = ticks;` with comment "1 tick = 1ms"
- Line 767: the `ms / 1000` for seconds and `(ms % 1000) * 1_000_000` for nanoseconds remain correct

**src/kernel/sys/syscall/process/scheduling.zig** (sys_gettimeofday fallback, around lines 879-884):
- Line 879: change `ticks *| 10` to `ticks` (same reasoning as above, 1 tick = 1ms)
- Lines 881-884: `ms / 1000` for seconds and `(ms % 1000) * 1000` for usec remain correct

**src/kernel/sys/syscall/process/scheduling.zig** (sys_clock_getres, around lines 816-828):
- Line 816 comment: change "10ms" to "1ms"
- Line 824 comment: change "10ms resolution" to "1ms resolution"
- Line 827: change `.tv_nsec = 10_000_000` to `.tv_nsec = 1_000_000`
- Line 827 comment: change "10ms" to "1ms"

**src/kernel/sys/syscall/process/scheduling.zig** (ppoll nfds=0 timeout, around line 297):
- Line 297: change `const ticks = us / 10_000;` to `const ticks = us / 1_000;` and update comment to "1ms per tick"

**src/kernel/sys/syscall/misc/posix_timer.zig** (TICK_NS constant, line 23):
- Change `const TICK_NS: u64 = 10_000_000;` to `const TICK_NS: u64 = 1_000_000;`
- Update comment from "10ms per tick" to "1ms per tick"
- Update module-level doc comment (line 12) from "10ms tick granularity (100 Hz scheduler tick)" to "1ms tick granularity (1000 Hz scheduler tick)"

**src/kernel/sys/syscall/misc/sysinfo.zig** (line ~34-36):
- Change comment from "tick_count / 100 (100 Hz = 10ms ticks)" to "tick_count / 1000 (1000 Hz = 1ms ticks)"
- Change `@divTrunc(ticks, 100)` to `@divTrunc(ticks, 1000)`

**src/kernel/sys/syscall/io/timerfd.zig** (comment only, line 19):
- Change "10ms granularity" to "1ms granularity" in the module-level comment
- Line ~102 comment: change "10ms resolution" to "1ms resolution"
- Line ~104: change `ticks *| 10` to `ticks` with comment "1 tick = 1ms"

IMPORTANT: Do NOT change the RR quantum (scheduling.zig line 230, `100_000_000` ns = 100ms). That is the scheduling time slice, not the tick period.
  </action>
  <verify>
`zig build -Darch=x86_64` and `zig build -Darch=aarch64` both compile without errors.

Run x86_64 tests to verify timer-related tests still pass:
```
ARCH=x86_64 ./scripts/run_tests.sh
```
Look specifically for: testNanosleepBasic, testClockGetresMonotonic, testTimerSetGetTime, testTimerSignalDelivery -- these are the most tick-sensitive tests.
  </verify>
  <done>
All scheduler, time syscall, POSIX timer, sysinfo, and timerfd code uses 1ms tick arithmetic. clock_getres reports 1_000_000 ns (1ms). POSIX timer TICK_NS is 1_000_000. Scheduler alarm math uses 1000 ticks/sec. Existing tests pass on x86_64.
  </done>
</task>

</tasks>

<verification>
1. Build both architectures: `zig build -Darch=x86_64 && zig build -Darch=aarch64`
2. Run x86_64 test suite: `ARCH=x86_64 ./scripts/run_tests.sh`
3. Grep for stale 100Hz references in modified files: `grep -n "100.*Hz\|10ms\|10_000_000\|\/.*100\|\\*.*100" <modified files>` -- should find no stale tick-period references (only unrelated uses of 100 like RR quantum or port numbers)
4. Verify clock_getres reports 1ms: check that scheduling.zig sys_clock_getres returns tv_nsec=1_000_000
5. Verify POSIX timer TICK_NS is 1_000_000: check posix_timer.zig constant
</verification>

<success_criteria>
- Both architectures compile cleanly with 1000Hz timer configuration
- x86_64 test suite passes (especially time_ops and posix_timer tests)
- clock_getres reports 1ms (1_000_000 ns) resolution
- No remaining 10ms/100Hz hardcoded values in the modified files (except unrelated constants)
</success_criteria>

<output>
After completion, create `.planning/phases/33-timer-resolution-improvement/33-01-SUMMARY.md`
</output>
