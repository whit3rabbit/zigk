---
phase: 12-sfs-feature-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fs/sfs/types.zig
  - src/fs/sfs/ops.zig
  - src/fs/sfs/root.zig
autonomous: true

must_haves:
  truths:
    - "Hard links can be created on SFS via link/linkat (two entries share same start_block)"
    - "Unlinking a hard-linked file decrements nlink; blocks freed only when nlink reaches 0"
    - "All directory entries sharing start_block always have identical nlink values"
    - "File timestamps (atime, mtime) can be set via utimensat/futimesat on SFS files"
    - "sfsStat returns actual mtime/atime values from disk (not always 0)"
  artifacts:
    - path: "src/fs/sfs/ops.zig"
      provides: "sfsLink, sfsSetTimestamps functions"
      contains: "fn sfsLink"
    - path: "src/fs/sfs/types.zig"
      provides: "DirEntry with nlink and atime fields"
      contains: "nlink"
    - path: "src/fs/sfs/root.zig"
      provides: "FileSystem wiring for link and set_timestamps"
      contains: ".link = sfs_ops.sfsLink"
  key_links:
    - from: "src/fs/sfs/root.zig"
      to: "src/fs/sfs/ops.zig"
      via: "sfsLink, sfsSetTimestamps function pointers in FileSystem struct"
      pattern: "\\.link = sfs_ops\\.sfsLink"
    - from: "src/fs/sfs/ops.zig"
      to: "src/fs/sfs/types.zig"
      via: "DirEntry.nlink field for hard link tracking"
      pattern: "entry\\.nlink"
    - from: "sfsUnlink in src/fs/sfs/ops.zig"
      to: "all DirEntry slots sharing start_block"
      via: "scan all 64 directory entries to find siblings with same start_block, decrement their nlink in lockstep"
      pattern: "sibling\\.start_block == target_start_block"
    - from: "sfsLink in src/fs/sfs/ops.zig"
      to: "all DirEntry slots sharing start_block"
      via: "after creating new entry, update ALL entries with same start_block to new nlink value"
      pattern: "entry\\.start_block == old_entry\\.start_block"
---

<objective>
Implement SFS hard link creation and file timestamp modification support.

Purpose: Enables link/linkat syscalls to create hard links on SFS, and utimensat/futimesat to modify file timestamps. Both features require DirEntry changes (nlink field, atime field) and new SFS operation callbacks wired through the VFS FileSystem interface.

Output: Working sfsLink and sfsSetTimestamps, DirEntry with nlink/atime fields, sfsStat returning real timestamps, sfsUnlink respecting nlink count with global nlink synchronization across all entries sharing start_block.
</objective>

<execution_context>
@/Users/whit3rabbit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/whit3rabbit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sfs-deadlock-resolution/11-01-SUMMARY.md

@src/fs/sfs/types.zig
@src/fs/sfs/ops.zig
@src/fs/sfs/root.zig
@src/fs/vfs.zig
@src/fs/meta.zig
@src/kernel/sys/syscall/fs/fs_handlers.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DirEntry with nlink and atime, update existing operations</name>
  <files>src/fs/sfs/types.zig, src/fs/sfs/ops.zig</files>
  <action>
**In `src/fs/sfs/types.zig`:**

1. Add `nlink` and `atime` fields to `DirEntry` by repurposing padding bytes. The current layout is:
   - name[32] + start_block(4) + size(4) + flags(4) + mode(4) + uid(4) + gid(4) + mtime(4) = 60 bytes
   - _pad: [128 - 60]u8 = 68 bytes of padding

   New layout (still 128 bytes total):
   - name[32] + start_block(4) + size(4) + flags(4) + mode(4) + uid(4) + gid(4) + mtime(4) + atime(4) + nlink(4) + _pad[128 - 68]u8 = 128 bytes

   Add these fields BEFORE `_pad`:
   ```
   atime: u32,    // Access time (Unix timestamp)
   nlink: u32,    // Number of hard links (0 or 1 = single, >1 = hard-linked)
   _pad: [128 - 68]u8,
   ```

2. Add `isSymlink()` method to DirEntry:
   ```zig
   pub fn isSymlink(self: *const @This()) bool {
       return (self.mode & 0o170000) == 0o120000;
   }
   ```

**In `src/fs/sfs/ops.zig`:**

3. Update `sfsStat` to return actual mtime and atime from the DirEntry. Currently it returns 0 for all timestamps. In the `refreshMetadataFromDisk` function, add mtime and atime to the `RefreshedMetadata` struct (also update `SfsFile.RefreshedMetadata` in types.zig to include mtime and atime fields). Then in `sfsStat`, populate `stat.mtime` and `stat.atime` from the metadata.

4. Update `sfsOpen` O_CREAT path: When creating a new DirEntry, set `nlink = 1` and `atime = 0` in the new entry initialization.

5. Update `sfsMkdir`: When creating a new directory DirEntry, set `nlink = 1` and `atime = 0`.

6. Update `sfsUnlink` with GLOBAL nlink synchronization:

   Before clearing the directory entry, check `entry.nlink`. If nlink > 1 (the file has other hard links), do NOT delete or free blocks. Instead:

   a. Record the `target_start_block = entry.start_block` before modifying anything.
   b. Compute `effective_nlink = if (entry.nlink == 0) @as(u32, 1) else entry.nlink`.
   c. If `effective_nlink > 1`:
      - Clear the unlinked entry itself (set flags=0, name zeroed, etc.) so it no longer appears in the directory. Do NOT free the data blocks.
      - Decrement file_count in superblock.
      - **CRITICAL nlink sync:** Scan ALL 64 directory entry slots. For every other active entry (`flags != 0`) whose `start_block == target_start_block`, set that entry's `nlink = effective_nlink - 1`. This ensures all remaining hard links reflect the correct count.
      - Write back ALL modified directory blocks (the block containing the unlinked entry AND any blocks containing sibling entries whose nlink was updated).
      - Write superblock.
   d. If `effective_nlink <= 1`:
      - Existing deletion logic: clear entry, free blocks, decrement file_count, etc.

   This is the most important invariant: at all times, every active directory entry sharing the same `start_block` must have the same `nlink` value. Unlinking removes one entry and decrements the rest. Linking adds one entry and increments all.

7. Update `sfsStatPath` to include nlink in the returned FileMeta. Note: FileMeta in `src/fs/meta.zig` may not have a nlink field. If not, this is fine -- nlink is returned via sfsStat (fstat path) which populates the Stat struct directly. In sfsStat, set `stat.nlink` to `metadata.nlink` (or 1 if 0 for backward compat).
  </action>
  <verify>
Run `zig build -Darch=x86_64 && zig build -Darch=aarch64` to confirm both architectures compile. The DirEntry struct must remain exactly 128 bytes (check at compile time via `comptime { if (@sizeOf(DirEntry) != 128) @compileError("DirEntry size mismatch"); }`). Verify no existing test regressions by running `ARCH=x86_64 ./scripts/run_tests.sh`.
  </verify>
  <done>DirEntry has nlink and atime fields. sfsStat returns real mtime/atime. sfsUnlink respects nlink count with global synchronization: unlinking one hard link decrements nlink on ALL remaining entries sharing the same start_block, and only frees blocks when the last link is removed. sfsOpen and sfsMkdir initialize nlink=1. Builds on both architectures. No test regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement sfsLink and sfsSetTimestamps, wire into FileSystem</name>
  <files>src/fs/sfs/ops.zig, src/fs/sfs/root.zig</files>
  <action>
**In `src/fs/sfs/ops.zig`:**

1. Implement `sfsLink(ctx: ?*anyopaque, old_path: []const u8, new_path: []const u8) vfs.Error!void`:
   - Extract old_name and new_name (strip leading '/')
   - Validate both filenames with `isValidFilename`
   - Reject nested paths (no '/' in name) -- SFS is flat
   - Follow the established TOCTOU-safe pattern from sfsRename:
     a. Read directory UNLOCKED to find old_entry and check new_name does not exist
     b. Verify old entry is a regular file (not directory -- POSIX: hard links to directories not allowed, return EPERM equivalent, which is `AccessDenied`)
     c. Check old entry is NOT a symlink (hard links to symlinks are technically allowed in POSIX but simplify by rejecting for SFS MVP)
     d. Find a free slot for the new entry
     e. Under alloc_lock: re-read both blocks, validate old entry still exists with same name, validate free slot still free, validate new_name not taken
     f. Create new DirEntry: copy old entry's start_block, size, mode, uid, gid, mtime, atime. Set name=new_name, flags=1.
     g. **CRITICAL nlink sync:** Compute new_nlink = (if old_entry.nlink == 0 then 1 else old_entry.nlink) + 1. Set the new entry's nlink = new_nlink. Then scan ALL 64 directory entry slots and set nlink = new_nlink on EVERY active entry whose start_block == old_entry.start_block (including old_entry itself). This ensures all entries sharing the data have identical nlink.
     h. Increment file_count in superblock
     i. Release lock
     j. Write ALL modified directory blocks outside lock (old entry's block, new entry's block, and any other blocks containing sibling entries whose nlink was updated)
     k. Write superblock outside lock
     l. On failure: rollback file_count and nlink under lock

   Important: All entries sharing a start_block must always have the same nlink. When creating a new hard link, the count goes up for ALL of them, not just the old and new pair.

2. Implement `sfsSetTimestamps(ctx: ?*anyopaque, path: []const u8, atime_sec: i64, atime_nsec: i64, mtime_sec: i64, mtime_nsec: i64) vfs.Error!void`:
   - Extract name (strip leading '/')
   - Validate filename
   - Follow the established TOCTOU-safe pattern from sfsChmod:
     a. Read directory UNLOCKED to find entry
     b. Under alloc_lock: re-read block, validate entry still exists with same name
     c. If atime_sec != -1 (not UTIME_OMIT): set entry.atime = @truncate(@as(u64, @bitCast(atime_sec)))
     d. If mtime_sec != -1 (not UTIME_OMIT): set entry.mtime = @truncate(@as(u64, @bitCast(mtime_sec)))
     e. Release lock
     f. Write block outside lock
   - Note: SFS DirEntry stores timestamps as u32 Unix seconds. Nanosecond precision is not stored (lossy, acceptable for SFS).

**In `src/fs/sfs/root.zig`:**

3. Wire the new functions into the FileSystem struct returned by `init()`:
   ```zig
   .link = sfs_ops.sfsLink,
   .set_timestamps = sfs_ops.sfsSetTimestamps,
   ```
   Ensure the sfsSetTimestamps function signature matches the VFS `set_timestamps` callback: `fn(ctx: ?*anyopaque, path: []const u8, atime_sec: i64, atime_nsec: i64, mtime_sec: i64, mtime_nsec: i64) vfs.Error!void`.
  </action>
  <verify>
Run `zig build -Darch=x86_64 && zig build -Darch=aarch64` to confirm both architectures compile. Run `ARCH=x86_64 ./scripts/run_tests.sh` and confirm existing tests pass. The fs_extras.zig tests (testLinkatBasic, testUtimensatNull, testUtimensatSpecificTime, testFutimesatBasic, testFutimesatSpecificTime) should now PASS instead of SKIP (they currently skip on EROFS/ReadOnlyFilesystem because SFS did not wire these callbacks).
  </verify>
  <done>sfsLink creates hard links on SFS with global nlink synchronization (all entries sharing start_block get identical nlink). sfsSetTimestamps modifies atime/mtime. Both wired into FileSystem. linkat and utimensat/futimesat tests pass on SFS (no longer skip). Both architectures build and pass tests.</done>
</task>

</tasks>

<verification>
1. `zig build -Darch=x86_64`: Builds without errors
2. `zig build -Darch=aarch64`: Builds without errors
3. `ARCH=x86_64 ./scripts/run_tests.sh`: All previously passing tests still pass
4. `@sizeOf(DirEntry)` == 128 (verified at compile time)
5. testLinkatBasic: PASS (not SKIP) -- creates hard link on SFS, verifies both paths accessible
6. testUtimensatNull: PASS (not SKIP) -- sets timestamps to current time
7. testUtimensatSpecificTime: PASS (not SKIP) -- sets specific timestamp values
8. testFutimesatBasic: PASS (not SKIP) -- sets timestamps via futimesat
9. nlink invariant: After link(A, B), stat(A).nlink == stat(B).nlink == 2. After link(A, C), all three have nlink == 3. After unlink(B), A and C have nlink == 2. After unlink(C), A has nlink == 1.
</verification>

<success_criteria>
- Hard links work on SFS: link/linkat creates a second directory entry pointing to same data blocks
- Unlinking a hard-linked file decrements nlink on ALL remaining entries sharing start_block; blocks freed only when last link removed
- All entries sharing a start_block always have identical nlink values (global synchronization invariant)
- utimensat/futimesat set atime/mtime on SFS files (stored as u32 seconds)
- sfsStat returns actual mtime/atime values from disk
- Both x86_64 and aarch64 build and pass tests
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/12-sfs-feature-expansion/12-01-SUMMARY.md`
</output>
