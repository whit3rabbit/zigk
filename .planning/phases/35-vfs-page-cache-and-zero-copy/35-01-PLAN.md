---
phase: 35-vfs-page-cache-and-zero-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kernel/fs/page_cache.zig
  - src/kernel/fs/fd.zig
  - src/kernel/core/init_fs.zig
  - build.zig
autonomous: true
requirements: [ZCIO-01]

must_haves:
  truths:
    - "Page cache stores file data in physical pages indexed by (file_id, page_offset)"
    - "Pages are reference counted and freed when no longer referenced"
    - "Page cache lookup returns existing pages without re-reading from backing store"
    - "Page cache supports read-ahead by populating adjacent pages on miss"
    - "Page cache supports write-back by marking pages dirty and flushing to backing store"
    - "FdTable.close flushes dirty pages and invalidates cache on last close of a file"
  artifacts:
    - path: "src/kernel/fs/page_cache.zig"
      provides: "Core page cache module with CachedPage, PageCache struct, and global cache"
      min_lines: 200
    - path: "src/kernel/fs/fd.zig"
      provides: "FdTable.close extended with page cache writeback/invalidate"
      contains: "page_cache"
    - path: "build.zig"
      provides: "page_cache_module registered with all required imports"
      contains: "page_cache_module"
  key_links:
    - from: "src/kernel/fs/page_cache.zig"
      to: "pmm"
      via: "allocZeroedPages/freePages for backing physical pages"
      pattern: "pmm\\.allocZeroedPages|pmm\\.freePages"
    - from: "src/kernel/fs/page_cache.zig"
      to: "hal"
      via: "physToVirt for kernel access to cached pages"
      pattern: "hal\\.paging\\.physToVirt"
    - from: "src/kernel/fs/fd.zig"
      to: "src/kernel/fs/page_cache.zig"
      via: "FdTable.close calls writeback/invalidate on file close"
      pattern: "page_cache"
    - from: "build.zig"
      to: "src/kernel/fs/page_cache.zig"
      via: "Module registration enabling @import(\"page_cache\")"
      pattern: "page_cache_module"
---

<objective>
Build the VFS page cache infrastructure that stores file data in physical pages, enabling zero-copy data transfer between file descriptors.

Purpose: Replace the 64KB heap-allocated kernel buffers currently used by splice/sendfile/tee/copy_file_range with a proper page cache. This is the foundation that Plan 02 will build on.

Output: `src/kernel/fs/page_cache.zig` with complete page cache API, `fd.zig` extended with page cache cleanup on close, `build.zig` with page_cache_module registration.
</objective>

<execution_context>
@/Users/whit3rabbit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/whit3rabbit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/kernel/fs/fd.zig
@src/kernel/fs/pipe.zig
@src/kernel/mm/pmm.zig
@src/fs/vfs.zig
@src/kernel/core/init_fs.zig
@build.zig
@src/kernel/sys/syscall/io/splice.zig
@src/kernel/sys/syscall/io/read_write.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create page cache module with core data structures and API</name>
  <files>src/kernel/fs/page_cache.zig</files>
  <action>
Create `src/kernel/fs/page_cache.zig` implementing a global file page cache.

**Data structures:**

1. `CachedPage` struct:
   - `phys_addr: u64` -- physical address of the 4KB page
   - `file_id: u64` -- identifies the file (use FileDescriptor.file_identifier)
   - `page_offset: u64` -- page-aligned offset within the file (byte offset / PAGE_SIZE)
   - `ref_count: std.atomic.Value(u32)` -- reference count, starts at 1 when inserted
   - `dirty: bool` -- true if page has been modified and needs write-back
   - `valid: bool` -- true if page contains valid data (vs allocated but not yet populated)
   - `next: ?*CachedPage` -- for hash chain linking

2. `PageCache` struct -- global singleton:
   - Hash table of CachedPage entries indexed by (file_id, page_offset)
   - Use a fixed-size hash table: `HASH_BUCKETS = 256` entries, each bucket is a linked list head (`?*CachedPage`)
   - `lock: sync.Spinlock` -- protects the hash table
   - `total_pages: usize` -- count of cached pages for statistics
   - `MAX_CACHED_PAGES: usize = 1024` -- eviction threshold (4MB of cached data)

**Hash function:** `hash(file_id, page_offset) = ((file_id *% 0x9E3779B97F4A7C15) ^ (page_offset *% 0x517CC1B727220A95)) % HASH_BUCKETS`

**Public API:**

- `pub fn init() void` -- initialize the global page cache (clear hash table)

- `pub fn getPages(file_id: u64, offset: u64, len: usize, read_fn: ?ReadFn, fd: ?*FileDescriptor) ![]PageRef`
  - Returns an array of `PageRef` structs covering the requested byte range
  - `PageRef = struct { page: *CachedPage, offset_in_page: usize, len: usize }`
  - For each page in the range: lookup in hash table, if found increment ref_count and return
  - On cache miss: allocate a physical page via `pmm.allocZeroedPages(1)`, populate via `read_fn` if provided, insert into hash table
  - Read-ahead: when a miss occurs, also prefetch the NEXT page (1-page read-ahead window) if within file bounds
  - The caller array should be allocated from heap (caller frees via `releasePages`)
  - Return error.ENOMEM if PMM or heap allocation fails
  - **Locking for read_fn calls:** When read_fn is provided with an fd, getPages must acquire fd.lock, save the current fd.position, seek to the target page offset, call read_fn to fill the page, restore fd.position, then release fd.lock. This prevents corrupting the fd's seek position for concurrent users.

- `pub fn releasePages(refs: []PageRef) void`
  - Decrements ref_count on each page
  - Frees the PageRef array from heap
  - Does NOT evict pages (they stay cached with ref_count=0 for future hits)

- `pub fn markDirty(page: *CachedPage) void`
  - Sets `dirty = true` on the page

- `pub fn writeback(file_id: u64, write_fn: WriteFn, fd: *FileDescriptor) void`
  - Iterates all pages for file_id, writes dirty pages back via write_fn, clears dirty flag
  - Called on fsync or file close

- `pub fn invalidate(file_id: u64) void`
  - Removes all pages for file_id from the cache
  - Frees physical pages for entries with ref_count == 0
  - Entries with ref_count > 0 are marked invalid (will be freed when released)

- `pub fn getPageData(page: *CachedPage) [*]u8`
  - Returns kernel virtual address for the page's physical memory
  - Uses `hal.paging.physToVirt(page.phys_addr)` to get HHDM virtual address

**Type definitions:**
- `ReadFn = *const fn (fd: *FileDescriptor, buf: []u8) isize` -- same as FileOps.read
- `WriteFn = *const fn (fd: *FileDescriptor, buf: []const u8) isize` -- same as FileOps.write
- `PageRef = struct { page: *CachedPage, offset_in_page: usize, len: usize }` -- a reference to a range within a cached page

**Eviction (simple LRU-approximation):**
When `total_pages >= MAX_CACHED_PAGES` and a new page is needed, scan buckets for pages with `ref_count == 0` and `dirty == false`. Free the first one found (FIFO within bucket). If all unreferenced pages are dirty, write them back first, then free. If no unreferenced pages exist, return error.ENOMEM.

**Thread safety:** All hash table operations (lookup, insert, remove) must hold `lock`. Physical page data access after getPages does NOT require the lock (ref_count prevents eviction).

**Lock ordering:** page_cache.lock should be acquired AFTER FileDescriptor.lock (higher number in the lock ordering hierarchy). Add a comment noting this in page_cache.zig. When getPages needs to call read_fn, it must NOT hold page_cache.lock while calling read_fn (acquire fd.lock, do I/O, release fd.lock, then acquire page_cache.lock to insert the page).

**Imports:** Use `@import("pmm")`, `@import("hal")`, `@import("heap")`, `@import("sync")`, `@import("fd")` (for FileDescriptor type), `@import("console")` (for debug/warn logging). Follow the same import pattern as pipe.zig.

**Important:** Use checked arithmetic (`std.math.add`, `std.math.mul`) for all offset/size calculations per CLAUDE.md security rules. Zero-initialize allocated pages via `pmm.allocZeroedPages` to prevent information leaks.
  </action>
  <verify>
`zig build -Darch=x86_64` compiles successfully after Task 2 completes (Task 1 alone will not compile because build.zig module registration happens in Task 2).
  </verify>
  <done>
`page_cache.zig` exists with CachedPage, PageCache, PageRef types, init/getPages/releasePages/markDirty/writeback/invalidate/getPageData API functions. Locking instructions for read_fn are specified (acquire fd.lock, save/restore position).
  </done>
</task>

<task type="auto">
  <name>Task 2: Register page_cache module in build.zig and integrate with FdTable.close and init</name>
  <files>build.zig, src/kernel/fs/fd.zig, src/kernel/core/init_fs.zig</files>
  <action>
1. **build.zig -- create page_cache_module and wire imports:**

   After the `pipe_module` definition (around line 1180), add:

   ```zig
   const page_cache_module = b.createModule(.{
       .root_source_file = b.path("src/kernel/fs/page_cache.zig"),
       .target = kernel_target,
       .optimize = optimize,
   });
   page_cache_module.addImport("pmm", pmm_module);
   page_cache_module.addImport("hal", hal_module);
   page_cache_module.addImport("heap", heap_module);
   page_cache_module.addImport("sync", sync_module);
   page_cache_module.addImport("fd", fd_module);
   page_cache_module.addImport("console", console_module);
   ```

   Then wire page_cache_module into modules that need it:

   - `fd_module.addImport("page_cache", page_cache_module);` -- so fd.zig can call writeback/invalidate
   - `syscall_io_module.addImport("page_cache", page_cache_module);` -- so splice.zig and read_write.zig can use getPages (needed by Plan 02)
   - `kernel.root_module.addImport("page_cache", page_cache_module);` -- so init_fs.zig (compiled as part of kernel root) can call page_cache.init()

   Find these existing definitions in build.zig and add the import lines near where the other imports for each module are grouped.

2. **fd.zig -- add page cache writeback/invalidate to FdTable.close():**

   Add import at top of fd.zig:
   ```zig
   const page_cache = @import("page_cache");
   ```

   In `FdTable.close()` (around line 336), AFTER the `if (fd.unref())` check returns true and BEFORE calling `close_fn`, add page cache cleanup:

   ```zig
   if (fd.unref()) {
       // Flush dirty page cache entries and invalidate cached pages for this file
       if (fd.file_identifier != 0) {
           if (fd.isWritable()) {
               if (fd.ops.write) |write_fn| {
                   page_cache.writeback(fd.file_identifier, write_fn, fd);
               }
           }
           page_cache.invalidate(fd.file_identifier);
       }

       // Existing close_fn call follows...
       if (fd.ops.close) |close_fn| {
           _ = close_fn(fd);
       }
       // ... rest of existing close logic unchanged
   }
   ```

   The writeback MUST happen before close_fn because close_fn may release the underlying file handle. The invalidate can happen before or after close_fn since it only removes cache entries.

   Do NOT modify `unref()` itself -- unref only handles refcounting and flock release. The page cache lifecycle belongs in FdTable.close() where all other cleanup (inotify hooks, VFS close hooks, destroy) already lives.

3. **init_fs.zig -- call page_cache.init() during filesystem initialization:**

   Add import at top of `src/kernel/core/init_fs.zig`:
   ```zig
   const page_cache = @import("page_cache");
   ```

   In the `initVfs()` function, call `page_cache.init()` right after `fs.vfs.Vfs.init()` (line 25) and before the mount calls:
   ```zig
   // Initialize VFS singleton
   fs.vfs.Vfs.init();

   // Initialize page cache for file data caching
   page_cache.init();

   // Mount InitRD at /
   // ...
   ```

   This ensures the page cache is ready before any files are opened.

**Important considerations:**
- The close path must NOT hold the page_cache lock for extended periods. writeback acquires/releases lock per page, then invalidate does a single pass.
- Lock ordering: page_cache.lock is acquired AFTER fd.lock (higher number in ordering).
- The init_fs.zig file is compiled as part of the kernel root module, so it needs `page_cache` imported at the kernel root module level in build.zig.
  </action>
  <verify>
`zig build -Darch=x86_64` and `zig build -Darch=aarch64` both compile successfully. Run `zig build test-kernel` to verify existing tests still pass (the page cache is additive -- existing code paths should not change behavior).
  </verify>
  <done>
build.zig has page_cache_module registered with all required imports (pmm, hal, heap, sync, fd, console) and wired to fd_module, syscall_io_module, and kernel root. FdTable.close() calls page_cache.writeback and page_cache.invalidate after unref returns true, before close_fn. page_cache.init() is called in initVfs() after VFS.init(). Both architectures compile. Existing tests unaffected.
  </done>
</task>

</tasks>

<verification>
- `zig build -Darch=x86_64` compiles without errors
- `zig build -Darch=aarch64` compiles without errors
- `zig build test-kernel` passes (no regressions)
- `src/kernel/fs/page_cache.zig` exists with all specified API functions
- `build.zig` has page_cache_module with imports for pmm, hal, heap, sync, fd, console
- `build.zig` wires page_cache to fd_module, syscall_io_module, and kernel root
- `src/kernel/fs/fd.zig` FdTable.close() calls page_cache writeback/invalidate on file close
- `src/kernel/core/init_fs.zig` calls page_cache.init() during VFS initialization
</verification>

<success_criteria>
Page cache infrastructure exists and compiles on both architectures. The module provides getPages/releasePages/markDirty/writeback/invalidate API. build.zig registers page_cache_module with proper imports and wiring. FdTable.close() flushes dirty pages and invalidates cached pages when the last reference is closed. Page cache is initialized during VFS init in init_fs.zig. No existing test regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/35-vfs-page-cache-and-zero-copy/35-01-SUMMARY.md`
</output>
