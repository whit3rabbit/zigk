# GRUB2 configuration for ZigK microkernel
# Uses Multiboot2 protocol to boot kernel

set timeout=3
set default=0

menuentry "ZigK Microkernel" {
    multiboot2 /boot/kernel.bin
    boot
}

menuentry "ZigK Microkernel (serial debug)" {
    multiboot2 /boot/kernel.bin
    boot
}
